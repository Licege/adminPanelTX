import React from 'react';

const Settings = (settings, global_settings) => {
    console.log(settings)
    console.log(global_settings)
    return (
        <div>
            <h1>Настройки</h1>
        </div>
    )
};

export default Settings;